/*
    TaskTouch

    Detects double tap
    Detects swipe left or swipe right
    Detects tap, which signals TaskLCD to turn on if LCD screen is off
*/

void TaskTouch(void *pvParameters)
{

  for(;;)
  {

  }
}