/*
    TaskLCD

    Outputs user roll number
    Outputs dice type if user double tapping screen
    Outputs new type type if user swipes screen left or right
    Dice types are D4, D6, D8, D10, D12, D20
    Outputs low battery message
    Turns off to save power when now being used for a while
*/

void TaskLCD(void * pvParameters)
{

  for(;;)
  {

  }
}